<?xml version="1.0" encoding="utf-8"?>
<!-- Custom scanner layout. ZXing's stock CaptureActivity is landscape-locked with no
     chrome at all — no title, no way back except the system gesture. -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:zxing="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#171717">

    <com.journeyapps.barcodescanner.DecoratedBarcodeView
        android:id="@+id/zxing_barcode_scanner"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        zxing:zxing_framing_rect_width="260dp"
        zxing:zxing_framing_rect_height="260dp"
        zxing:zxing_preview_scaling_strategy="fitCenter"
        zxing:zxing_use_texture_view="true"/>

    <!-- Header over the preview: back + title. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#CC171717"
        android:paddingTop="36dp"
        android:paddingBottom="12dp"
        android:paddingStart="8dp"
        android:paddingEnd="16dp">

        <ImageButton
            android:id="@+id/scan_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:tint="#FFFFFF"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Scan pairing code"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"/>
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:padding="24dp"
        android:gravity="center"
        android:text="Point the camera at the QR shown in the Node Remote panel in Basecamp"
        android:textColor="#A4A4A4"
        android:textSize="14sp"/>
</RelativeLayout>
